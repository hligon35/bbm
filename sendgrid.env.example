# Example only. Do NOT commit real keys.
#
# Usage (PowerShell):
#   Get-Content sendgrid.env | ForEach-Object { $_ }  # (or source it in your shell of choice)
#
# SendGrid API key used by the Cloudflare Worker email sender (if configured)
export SENDGRID_API_KEY='YOUR_SENDGRID_API_KEY'
